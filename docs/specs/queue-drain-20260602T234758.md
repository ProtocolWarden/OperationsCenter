<!-- generated_by_run: e84168a6-874f-426d-9a05-78c37aa7b2c3 -->

---
campaign_id: 2d7f9c1a-6e5b-41a3-9f28-4c1e8d2b7f5a
slug: queue-drain-20260602T234758
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - reviewer
  - merge_decision
  - verdict_gate
  - pr_state_machine
  - ci_precondition
status: active
created_at: 2026-06-02T23:47:58Z
---

## Overview

Recent PRs (#224, #226, #229) hardened the reviewer verdict-gate and merge-decision logic, introducing CI-green as a hard merge precondition and an adversarial audit of the verdict-gate loop. The verdict consolidation, merge-decision state machine, and CI-green gate now form a critical path for auto-merge safety. This campaign adds comprehensive integration tests for verdict→decision→merge workflows, validates the CI-green gate under realistic PR state transitions, and instruments decision outcomes with structured metrics to enable operator monitoring of merge safety.

## Goals

1. **Verdict consolidation and merge-decision state machine** — Add integration tests for the complete verdict-gate flow: audit verdict collection (pass/fail), verdict consolidation across lanes, merge-decision outcome (ready/blocked/retry), and state transitions. Cover: unanimous pass → merge, mixed verdicts with override logic, retry conditions, timeout recovery. Test both happy path (CI green, no objections) and safety paths (pending CI, stale verdicts, verdict changes mid-flight). Success: 6–8 integration tests, all consolidation paths validated, no decision-state regressions.

2. **CI-green as merge precondition validation** — Add tests verifying that CI-green is now a hard blocker for merge approval: PR cannot merge until all CI checks pass and green status is confirmed. Cover scenarios: CI pending (merge blocked), CI red (merge blocked, retry after fix), CI green (merge allowed), CI status stale (re-check before merge). Validate that gate prevents premature merges. Success: 5–6 tests covering all CI state transitions, zero false-pass merges in test suite.

3. **PR state machine and transition validation** — Add tests for valid and invalid PR state transitions (draft→ready, ready→in-review, in-review→approved, approved→can-merge). Cover concurrent state changes, verdict updates during review, merge cancellation, and rollback scenarios. Verify state consistency across verdict updates. Success: 7–9 tests for state machine, edge cases like orphaned states covered, no deadlock scenarios.

4. **Merge-decision instrumentation and safety metrics** — Instrument verdict consolidation, merge-decision outcomes, and CI-green gate checks with structured logging and metrics. Add counters for decision outcomes (approved/blocked/retry) and histograms for decision latency. Enable operators to track merge safety health and detect anomalies (e.g., unusual retry rates, CI-green delays). Success: metrics exported, baseline decision latency <500ms, operator dashboard ready.

## Constraints

- **Scope:** Integration tests, instrumentation, and validation only — no changes to verdict consolidation logic, merge-decision algorithm, or CI-green gate policy.
- **Allowed paths:** Add tests under `tests/integration/reviewer/`; add instrumentation to `src/operations_center/reviewer/` merge_decision and verdict_consolidation modules; add metrics exports and operator docs.
- **Avoid:** Refactoring verdict-gate state machine, changing override conditions, modifying audit logic (PR #229 scope), or live GitHub API calls during tests. Use fixtures for PR state and audit verdicts.
- **State realism:** PR states must mirror actual GitHub PR states; verdict collections must use real audit verdict schemas (pass/fail/pending). Avoid synthetic or oversimplified state representations.
- **Test isolation:** All tests deterministic, no external API calls, no time-dependent assertions. Use mocked GitHub API responses and synthetic PR/verdict fixtures. Tests must pass in parallel.

## Success Criteria

1. **Verdict consolidation tests passing:** All 6–8 integration tests for verdict collection, consolidation, and merge-decision outcomes pass deterministically. Verify that unanimous pass allows merge, mixed verdicts respect override logic, and retry conditions are correctly detected.

2. **CI-green gate validation:** All 5–6 tests for CI-green precondition pass. Confirm that CI pending/red state blocks merge, CI green state allows merge, and stale CI status triggers re-check. Zero false-pass merges in test coverage.

3. **PR state machine coverage:** All 7–9 state transition tests pass. Valid transitions (draft→ready→in-review→approved→can-merge) work correctly; invalid transitions are rejected. Concurrent state changes and rollback scenarios handled safely. No deadlock or orphaned state scenarios.

4. **Merge-decision instrumentation operational:** Metrics for decision outcomes (approved/blocked/retry counts) and decision latency (histogram <500ms baseline) exported and queryable. Operator guide documents expected latency, anomaly triggers, and debugging steps. Structured logs show decision chain (verdict A passed → B pending → consolidated → merge blocked, reason: CI pending).

5. **Test suite stability and regression detection:** All 18–23 new integration tests pass in 3 consecutive CI runs with zero flakiness. Existing reviewer and merge-decision tests remain unchanged and passing. Coverage gate maintained at ≥85%. Integration test suite runtime <2 min total.
