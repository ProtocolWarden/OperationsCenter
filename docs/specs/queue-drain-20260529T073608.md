<!-- generated_by_run: 272e14ca-e465-4e15-9fa4-b3f7d7d0ef85 -->
---
campaign_id: 5a2c3f9e-b7d1-4c2a-9f5e-8d1b6e3a2c47
slug: queue-drain-20260529T073608
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - src/operations_center/spec_author/
  - tests/spec_author/
status: active
created_at: 2026-05-29T07:36:08+00:00
---

## Overview

Harden the recently developed spec_author infrastructure with comprehensive test coverage for phase orchestration, state machine transitions, and error handling. This campaign closes test gaps in critical spec-authoring logic that drives queue-drain campaign creation and lifecycle management.

## Goals

1. **Phase Orchestrator State Machine Tests** — Add integration tests covering terminal state detection, phase advancement conditions, and campaign promotion logic for all three phases (implement → test → improve → archived). Target: 8–12 tests covering success paths, edge cases (blocked tasks, mixed task states), and error scenarios.

2. **Spec Parser & Validation Tests** — Add tests for malformed spec files (invalid YAML front matter, missing required fields, invalid campaign_id/slug formats, out-of-bounds goal counts). Target: 6–8 tests covering parse errors, field validation, and error message clarity.

3. **Campaign State Persistence Tests** — Add tests for state manager round-trip (write state, reload from disk, verify consistency) and concurrent access scenarios. Target: 4–6 tests covering JSON load/save, corruption recovery, and race conditions.

## Constraints

- Do not modify spec_author API or data models; only add tests.
- Use existing mock Plane clients to avoid live API calls.
- Tests must run in <10s per goal; mark slow tests with `@pytest.mark.slow` if >1s.
- Reuse existing test fixtures (campaign_builder mock patterns) where possible.
- All tests must pass with 100% isolation (no shared state between test instances).

## Success Criteria

- Phase orchestrator tests achieve ≥80% code coverage for `phase_orchestrator.py` terminal-state and promotion logic.
- Parser and validation tests collectively cover all malformation types in the spec schema (yaml, fields, enums, ranges).
- State persistence tests verify round-trip correctness for min/max sized campaigns and confirm no data loss on write failures.
- All new tests pass in CI on the default branch within 5 executor runs or fewer.
- Test files are organized under `tests/spec_author/` following existing naming patterns.
