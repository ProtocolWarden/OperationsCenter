<!-- generated_by_run: acb69688-1863-4b5d-bf5a-e2c056a2bce8 -->

---
campaign_id: 3e7d4f1a-6b2c-4e9f-8a3d-5c1b7e9f2a4d
slug: queue-drain-20260602T162243
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - worker-backend
  - observability
  - error-handling
status: active
created_at: 2026-06-02T16:22:43Z
---

## Overview

Harden observability and error resilience in OperationsCenter's worker-backend subsystem following recent per-model cooldown work. The campaign addresses observability gaps in backend health monitoring, hardens edge cases in rate-limit fallback paths, and improves test coverage for model-aware backend decisions.

## Goals

1. **Implement comprehensive backend health metrics collection** — Export per-model backend capacity, cooldown state, and fallback invocations to Prometheus/alerting. Success: metrics scraped, alerting rules defined, no backend state blindness on deployment.

2. **Harden diff-too-large fallback and edge cases** — Add timeout guards, stale-response handling, and recovery for file-list fallback in `get_pr_diff()`. Ensure no silent failures or crash loops under malformed input. Success: all edge-case paths tested, no exceptions swallowed.

3. **Test coverage for model-aware backend selection** — Add unit/integration tests for model→backend mapping, cooldown gate logic, and opus fallback promotion. Cover happy path and cooldown-induced stalls. Success: coverage >90% for backend decision logic, all branches exercised.

4. **Document worker-backend rate-limit semantics** — Author `docs/backend-rate-limits.md` capturing per-model limits, cooldown behavior, and fallback priority. Include examples and failure modes. Success: on-call and future maintainers can diagnose backend stalls without code dive.

## Constraints

- **Backwards compatibility**: Metrics should not change existing backend behavior; add new instrumentation only.
- **No feature bloat**: Improvements strictly to observability and error paths; defer new backend selection strategies to future campaigns.
- **Test isolation**: New tests must not depend on live backend services or time-sensitive sleeps; use mocked state transitions.
- **Docs stay in tree**: Keep all documentation in `docs/` committed; no external wikis or lost context.

## Success Criteria

- All four goals have merged PRs to main.
- Prometheus metrics are emitted under `oc_backend_*` namespace with proper labels (model, backend_type, state).
- `get_pr_diff()` fallback path has 100% test coverage; no timeout or exception regressions in CI.
- Backend decision logic tests cover all three models (haiku, sonnet, opus) × all cooldown states (active, cooldown, ready).
- `docs/backend-rate-limits.md` is up-to-date and linked from README; includes rate-limit values and fallback flow diagram.
- Custodian audit passes; ruff lint clean; all pre-push hooks pass.
