<!-- generated_by_run: 7f51e0c6-5d4e-46bc-ae21-70ef6e94da7a -->

---
campaign_id: a7f2e8c1-3b4d-4f9a-8e2c-1d5a6b9f3c2e
slug: queue-drain-20260602T172812
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - src/operations_center/execution/backends
  - src/operations_center/board
  - worker_backend
  - cooldown
  - model_selection
status: active
created_at: 2026-06-02T17:28:12Z
---

## Overview

Recent commits added per-model worker-backend cooldowns to prevent cascading failures during rate limits (limit_kind + model tracking). The feature spans execution dispatch, board-unblock promotion gates, and controller rate-limit handling. Unit and integration tests are needed to ensure cooldown state transitions, fallback chains (sonnet → opus), and promotion gates work correctly across all scenarios.

## Goals

1. **Unit tests for cooldown state machine** — Add tests for per-(model, backend) cooldown entry, duration tracking, and expiration logic. Cover normal transitions, concurrent state updates, clock-skew edge cases, and invalid state rejections. Target ≥85% branch coverage in cooldown module.

2. **Integration tests for model fallback under cooldown** — Add tests verifying the model selection fallback chain (sonnet → opus) respects active cooldowns and that promotion gates in board-unblock correctly block R4AI upgrades when cooldown is active. Cover at least 3 scenarios: single model cooldown, multiple models cooldown, promotion-gate block.

3. **Rate-limit handler protocol compliance** — Add tests for controller rate-limit message parsing and model downgrade triggering. Verify that real per-model rate-limit responses correctly initiate cooldowns and don't trigger spurious fallbacks on non-rate-limit errors.

4. **Operational metrics for cooldown events** — Instrument cooldown entry, expiration, and promotion-gate blocks with structured logging and metrics. Add timer histograms for cooldown duration and counter for gate-blocked decisions to enable operator alerting.

## Constraints

- **Scope:** Unit tests, integration tests, and instrumentation only — no refactoring of cooldown state machine itself unless required for testability.
- **State model:** Cooldown state is per-(model, backend_name) pair; do not consolidate into single global or per-backend state.
- **Fallback chain:** Preserve canonical priority (sonnet → opus → codex); tests must validate this ordering is respected.
- **Board-unblock gate:** R4AI promotion must check cooldown state before permitting worker upgrade; gate logic is read-only in this campaign.
- **Avoid:** Changes to rate-limit thresholds, cooldown duration configuration, or executor dispatch policy.
- **Test location:** Add tests to `tests/execution/backends/` and `tests/board/` following existing pytest patterns.

## Success Criteria

1. **Cooldown state machine unit tests:** All transitions covered (no cooldown → active → expired), including edge cases (clock skew, concurrent updates, invalid states). Module reports ≥85% branch coverage. All tests pass in CI.

2. **Model fallback integration tests:** Fallback chain respects cooldowns in 3+ scenarios. Board-unblock promotion gate correctly blocks when cooldown active. All fallback tests pass; no regression in model selection throughput.

3. **Rate-limit handler tests:** Controller correctly parses per-model rate-limit messages and initiates model-specific cooldowns. Non-rate-limit errors do not trigger false cooldowns. Handler tests achieve ≥80% line coverage.

4. **Metrics and logging:** Cooldown events emit JSON logs with timestamps, model, backend name, and event type (entry/expiration/gate-blocked). Metrics include cooldown_duration_seconds histogram and promotion_gate_blocks_total counter. Operator can track cooldown health from logs.

5. **CI green:** All new tests pass locally and in CI. Existing execution and board tests remain unchanged and passing. No performance regression in cooldown state checks.
