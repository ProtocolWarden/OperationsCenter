<!-- generated_by_run: c81cd5c1-6fe3-40c1-adf5-f63adc4f191c -->

---
campaign_id: 3e4f2b1a-5c7d-4e2b-9a1f-8c6d2e5a7b9f
slug: queue-drain-20260530T110727
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - executor
  - backend
  - adapter
  - protocol_compliance
  - test_coverage
status: active
created_at: 2026-05-31T11:07:27Z
---

## Overview

Recent executor adapter work (CritiqueExecutorBackendAdapter protocol tests, ExecutionResult validation, DependencyReportValidator coverage) demonstrates the value of structural protocol-compliance testing for backend contracts. This campaign extends that pattern to cover team_executor and dag_executor adapters, hardens DependencyReportValidator edge cases, and adds error-path logging instrumentation to improve visibility into executor failure modes.

## Goals

1. **Add protocol-compliance tests for team_executor and dag_executor backends** — Add structural tests validating backend adapter interface contracts, parameter validation, and error propagation following the CritiqueExecutorBackendAdapter pattern. Cover normal dispatch, missing parameters, malformed payloads, and timeout rejection. Target ≥80% line coverage for both adapters.

2. **DependencyReportValidator edge-case coverage** — Add focused tests for circular dependency detection, missing required fields, malformed schema validation, and recovery behavior. Extend recent coverage work with explicit edge cases. Target ≥85% line coverage for validator module.

3. **Executor error logging and structured output** — Add tests for error paths in executor dispatch: timeout handling, transient failure recovery, partial result states, and error log formatting. Verify logs are JSON-structured and include dispatch ID, latency, and error classification for operator debuggability.

## Constraints

- **Scope:** Unit tests and instrumentation only — no refactoring of executor adapters themselves unless required to make tests cleaner.
- **Allowed paths:** Use existing test infrastructure (pytest, mocks, fixtures); add tests under `tests/executor/adapters/` and `tests/validators/`; modify logging in `src/operations_center/executor/adapters/` only.
- **Avoid:** Changes to adapter dispatch policy, governance rules, or backend selection logic; configuration system overhaul.
- **Target modules:** `src/operations_center/executor/adapters/` (team_executor, dag_executor), `src/operations_center/validators/dependency_report.py`, and executor error handlers.
- **Protocol tests:** Must validate interface contracts without mocking the backend — use real adapter instances with test payloads.

## Success Criteria

1. **Backend protocol-compliance tests:** Protocol tests added for team_executor and dag_executor with coverage for normal dispatch, parameter validation, malformed payloads, and timeouts. Both adapters report ≥80% line coverage. All tests pass in CI.

2. **DependencyReportValidator edge cases:** 4–6 new edge-case tests added (circular dependencies, missing fields, schema violations, recovery). Validator module reports ≥85% line coverage. All existing validator tests still pass.

3. **Error logging and structured output:** Error paths emit JSON logs with dispatch_id, latency_ms, error_code, and error_classification. Operator can extract failure modes from logs. 3–4 tests validate log structure and accuracy. No regression in executor throughput.

4. **CI green:** All new tests pass locally and in CI. Existing executor and adapter test suites remain unchanged and passing.
