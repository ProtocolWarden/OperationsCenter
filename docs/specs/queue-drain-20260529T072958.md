<!-- generated_by_run: 885286a7-2972-4bf1-95e1-aeff1ec1638e -->

---
campaign_id: 7a9f4c8e-3b2d-4e16-9c5f-1b8d2a5c9f3e
slug: queue-drain-20260529T072958
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - board-worker
  - task-promotion
  - validation
status: active
created_at: 2026-05-29T07:29:58Z
---

## Overview

Recent board-worker fixes show recurring issues in task promotion logic, workspace validation ordering, and PR gate detection across three subsystems. This campaign targets four concrete pain points identified in the past 5 commits: false-positive STALE_IN_REVIEW signals, thin-goal task re-promotion loops, type coverage gaps in the PR gate implementation, and baseline validation file handling. Each goal is scoped as an independent executor task (~1 hour) with measurable success criteria tied to test coverage and operational stability.

## Goals

1. **Eliminate false-positive STALE_IN_REVIEW detections** — Add explicit test coverage for `is_stale_in_review()` logic with open PR scenarios; ensure tasks with active PRs are never marked stale.

2. **Add regression test for thin-goal re-promotion loop** — Pin the fix from `bf68fcf` with an integration test that verifies `GOAL_BACKLOG_PROMOTE` correctly skips thin-goal tasks and does not re-promote them across cycles.

3. **Complete type checking coverage for PR gate** — Resolve remaining type errors in open-PR detection code (from `e746942`); ensure full type checking passes with `ty==0.0.40` pin on P6-affected files.

4. **Stabilize workspace validation file handling** — Add explicit ordering test for baseline validation checkpoint restoration before task-branch checkout (fix from `d25e567`); verify no dirty-tree failures on subsequent runs.

## Constraints

- Stay within the `src/operations_center/board_worker/` and `src/operations_center/observer/` subsystems; do not expand scope to other modules.
- All tests must run under existing CI (no new external dependencies).
- Type fixes must pass `ruff check`, `ty check`, and `mypy` without suppressions.
- Each goal is independent and completable in a single executor run (~1 hour max).

## Success Criteria

- Four PRs merged, one per goal (or fewer if bundled for cohesion).
- All new tests green in CI; no regression in existing suite (>3600 tests maintained).
- Type coverage complete: zero new `# type: ignore` comments; zero suppressions on P6 tool versions.
- Board-worker cycle count stable: no new false-positive promotions in next 3 cycles (observable via ops console log).
