<!-- generated_by_run: 21eca4ce-68ad-4f99-9877-6102b76502bb -->
---
campaign_id: 9c7a2e81-f3e4-4b87-91d8-3a5f0c2d6b9a
slug: queue-drain-20260529T070332
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - entrypoints/maintenance
  - board_worker
  - board_unblock
  - task_promotion
status: active
created_at: 2026-05-29T07:03:32Z
---

## Overview

Recent queue-drain completion (board status healthy) revealed vulnerabilities in board-unblock rule application and task promotion logic. Commits `d5330a4` (thin-goal skip), `bf68fcf` (loop prevention), and `a5a088d` (STALE_IN_REVIEW false-positive) document concrete fixes that lack test coverage. This campaign hardens queue-drain against re-occurrence by adding targeted tests for false-positive patterns, task promotion guards, and baseline state consistency.

## Goals

1. **Add board-unblock rule edge-case tests** — Create `tests/unit/entrypoints/maintenance/test_board_unblock_edge_cases.py` covering: STALE_IN_REVIEW with open PRs present (regression for `a5a088d`), label-matching with whitespace-in-value, staleness boundary conditions (exactly at threshold, just under, just over). Use parametrized tests for the three boundary cases per rule; verify no false-positives on common task states.

2. **Test thin-goal task promotion loop prevention** — Create `tests/unit/entrypoints/maintenance/test_thin_goal_safety.py` verifying that GOAL_BACKLOG_PROMOTE correctly skips thin-goal tasks (commit `d5330a4`) and that tasks carrying a promotion-skip marker cannot re-enter the promotion queue on subsequent drain cycles. Test both skip logic and marker persistence across cycle boundaries.

3. **Add task state consistency guards** — Extend or create `tests/unit/entrypoints/maintenance/test_task_state_machine.py` to verify that task.status transitions and task.promoted/skipped marker fields remain consistent; invalid status values fail at parse time; no unguarded field access silently succeeds. Validate the invariants: promoted tasks cannot be demoted, skipped tasks cannot be promoted in the same cycle.

4. **Verify baseline validation restoration before checkout** — Create `tests/integration/entrypoints/test_workspace_checkout_safety.py` that reproduces the scenario in commit `d25e567`: run a board-worker cycle, confirm `.baseline-validation.json` is restored before task-branch checkout, and verify no uncommitted state blocks the checkout. This ensures workspace pollution cannot stall future drain cycles.

## Constraints

- New unit tests go in `tests/unit/entrypoints/maintenance/` (tests 1–3); integration test in `tests/integration/entrypoints/` (test 4).
- All tests must be pure unit tests (use mocks/fixtures) except test 4, which may use file I/O and subprocess for board-worker invocation.
- Keep unit test files under 200 lines each to fit a single executor run.
- Do not modify production source code; add test files only.
- Test coverage must reach ≥ 80% for touched code paths; use `coverage.py` to measure.
- Follow existing test layout and fixture patterns from adjacent test files.

## Success Criteria

- All unit test files exist and pass: `pytest tests/unit/entrypoints/maintenance/test_board_unblock_edge_cases.py tests/unit/entrypoints/maintenance/test_thin_goal_safety.py tests/unit/entrypoints/maintenance/test_task_state_machine.py` with ≥ 6 tests per file.
- Integration test passes: `pytest tests/integration/entrypoints/test_workspace_checkout_safety.py` covering all four assertions (skip logic, marker persistence, state consistency, baseline restoration).
- Full suite exits 0: `pytest tests/` with zero regressions.
- Coverage.py reports ≥ 80% coverage on `entrypoints/maintenance/` modules touched by the tests.
