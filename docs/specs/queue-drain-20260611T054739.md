<!-- generated_by_run: 53c35904-8c50-4a32-9eac-f26363a769b7 -->

---
campaign_id: b9e4f2c8-1a7d-41e5-9c3b-6f2e8a5d1b3c
slug: queue-drain-20260611T054739
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - reviewer
  - process_lifecycle
  - timeout_recovery
  - ci_verdict
  - verdict_retraction
status: active
created_at: 2026-06-11T05:47:39Z
---

## Overview

Recent reviewer commits (d9afd797, 1cfbd0f) hardened process group cleanup and CI-green verdict trust logic to prevent deadlocks and false merges. Process groups now ensure clean timeout recovery without deadlocking pipe readers; CI-green verdicts are retracted when trust conditions fail. This campaign adds comprehensive test coverage for process lifecycle recovery, validates CI-green verdict consistency and retraction paths, and instruments verdict state transitions for operator observability.

## Goals

1. **Process group timeout recovery and deadlock prevention** — Add integration tests for fix-pass process group handling: cover normal completion, timeout with process-group kill, partial process recovery, SIGTERM handling, and pipe-deadlock prevention. Verify that timeouts safely clean up stray processes and readers without blocking. Include stress tests for concurrent fix-pass timeouts and edge cases near timeout boundaries. Success: 6–8 integration tests, all timeout scenarios covered, zero deadlock or process-leak scenarios in CI.

2. **CI-green verdict trust and retraction logic** — Add unit and integration tests validating CI-green verdict trust conditions: cover unanimous CI green with sufficient PR history, retraction when budget exhausted, retraction on CI status regression, and trust restoration on new green cycle. Verify that verdicts issued only when all trust conditions met and retracted when any condition fails. Success: 8–10 tests covering all trust/retract transitions, zero false-trust verdicts in test coverage.

3. **Verdict state machine and transition validation** — Add integration tests for verdict consolidation state transitions under CI changes: cover verdict issued→retracted→reissued cycles, concurrent verdict updates, stale verdict recovery, and no-verdict timeouts. Test edge cases: verdict issued just before CI fails, retraction during fix-pass, multiple concurrent retraction triggers. Success: 7–9 tests for state transitions, ≥85% branch coverage in verdict consolidation module.

4. **Reviewer verdict instrumentation and metrics** — Instrument verdict lifecycle (issued, retracted, reissued) with structured JSON logging and Prometheus metrics. Add counters for verdict outcomes (approved/blocked/retracted) and histograms for verdict persistence time. Export per-reason retraction breakdown to enable operator dashboard showing retraction causes (budget, CI-regression, escalation-clear). Success: Metrics exported, structured logs include verdict_state/trigger/reason fields, operator can diagnose why a verdict was retracted without code inspection.

## Constraints

- **Scope:** Test coverage and instrumentation only — no changes to verdict consolidation logic, CI-green gate policy, or timeout duration configuration.

- **Allowed paths:** Add tests under `tests/unit/reviewer/` and `tests/integration/reviewer/`; add instrumentation to `src/operations_center/reviewer/` verdict and process modules; modify logging/metrics only. Structured logging via standard Python logging; Prometheus metrics via `prom_client` or existing exporter.

- **Avoid:** Refactoring verdict state machine, changing CI-green thresholds, modifying timeout durations, or changing process cleanup policy. Use fixtures for process state and CI responses. Do not add new external service dependencies.

- **Process realism:** Tests must use realistic UNIX process group primitives, real timeout mechanics, and synthetic PR/CI state fixtures. Avoid synthetic stubs; exercise actual process signals (SIGTERM, SIGKILL). Tests must work cross-platform or declare POSIX-only boundaries.

- **Isolation:** All tests deterministic and parallel-safe. Use mocked GitHub API and clock-injection for time-dependent tests. No external processes spawned in CI; no flaky timing assumptions. Suites must complete within pytest wall-clock constraints.

- **Integration boundaries:** Verdict instrumentation must not modify the verdict consolidation return contract; logging and metrics are observability-only add-ons. No changes to verdict call sites or downstream consumer code in controller/adapter layers.

- **Code hygiene:** No breaking changes to public reviewer module APIs. All new test fixtures and helpers follow existing patterns in `tests/conftest.py` and `tests/fixtures/`. New tests must pass `custodian audit` and pre-push hooks (Custodian, ruff, license headers).

- **ContextLifecycle compliance:** Tests must not assume or depend on `CL_ANCHOR` being set; verdict state machine is agnostic to cognition lifecycle. Instrumentation must survive both anchored and unanchored deployments.

## Success Criteria

1. **Process timeout tests passing:** All 6–8 integration tests for process group handling and timeout recovery pass deterministically. Verify normal completion exits cleanly, timeouts trigger process group kills, partial recovery succeeds, concurrent timeouts don't interfere. Zero stray processes or pipe deadlocks in CI environment.

2. **CI-green verdict trust validation:** All 8–10 tests for verdict trust and retraction logic pass. Confirm CI-green verdicts issued only when trust conditions met, retracted when conditions fail (budget exhausted, CI regresses, escalation clears), and reissued when conditions recover. No false-trust verdicts in coverage.

3. **Verdict state machine coverage:** All 7–9 state transition tests pass, covering issued→retracted→reissued cycles, concurrent updates, stale recovery, and timeout handling. Verdict consolidation module reports ≥85% branch coverage. All edge cases (verdict during CI failure, retraction during fix-pass) validated.

4. **Verdict instrumentation operational:** Metrics for verdict outcomes (approved/blocked/retracted) and retraction reason breakdown exported via Prometheus. Structured JSON logs include verdict_state, trigger, and reason fields. Operator dashboard shows verdict health and retraction patterns. Post-mortems can reference run logs without code inspection.

5. **Test suite stability and coverage:** All 21–27 new tests pass in 3 consecutive CI runs with zero flakiness. Existing reviewer tests remain unchanged and passing. Coverage maintained at ≥85%. Integration test suite runtime <3 min total. Custodian audit passes; pre-push hooks clean.
