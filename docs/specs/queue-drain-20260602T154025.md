<!-- generated_by_run: 526adef5-aa75-4507-a277-d17c9db1c9d7 -->

---
campaign_id: a1c2e4d6-8f3b-4c2d-9e1a-b3c4d5e6f7a8
slug: queue-drain-20260602T154025
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - src/operations_center/observer/
  - src/operations_center/watching/
  - src/operations_center/review/
status: active
created_at: 2026-06-02T15:40:25Z
---

## Overview

Close gaps in Observer audit handling and Watching role validation following recent fixes in PR #213 and #217. Add systematic test coverage for validation-failure edge cases and improve error observability across the observer/watching boundary.

## Goals

1. Audit Observer error paths: verify all audit-failure cases from PR #213 merge (commit 4c74e1a3) have corresponding defensive unit tests; add fixtures for missed branches
2. Harden role-rejection logic: ensure watching.start_watch_role() rejects all unknown role types; add edge-case tests for concurrent role validation
3. Export validation-failure events: wire observer module to emit structured validation-failure metrics for downstream alerting
4. Regression-test review large-diff fallback: add integration test for review.get_pr_diff() 406 handling (commit 44fefeeb) under synthetic large-diff conditions

## Constraints

- Do not modify execution, controller, or backends subsystems — focus on observer/watching/review boundaries only
- Maintain backward compatibility with existing observer audit API
- Validation metrics must emit via existing channels (no new instrumentation points)
- All tests must complete in <45 min on CI (leave 15 min buffer for flakiness)
- Do not add new external dependencies

## Success Criteria

- All observer audit-failure code paths have ≥1 test case with branch coverage ≥95%
- watching.start_watch_role() unknown-role rejection branch has ≥2 test cases covering concurrent and sequential edge cases
- Observer module emits validation-failure events as structured metric records (not log strings)
- Review large-diff fallback test passes deterministically on CI with synthetic payloads ≥10MB
- All new tests pass on main with zero flakiness (3 consecutive green runs)
