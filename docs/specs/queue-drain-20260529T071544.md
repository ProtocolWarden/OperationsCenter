<!-- generated_by_run: 88f13c39-0899-43ca-92d5-8ae306092802 -->

---
campaign_id: c2f7e1a9-3d2b-4c8f-9a1e-7b5d8c3f2e1a
slug: queue-drain-20260529T071544
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - spec_author
  - campaign_builder
  - phase_orchestrator
status: active
created_at: 2026-05-29T07:15:44Z
---

## Overview

The `spec_author` subsystem — responsible for parsing campaign specs, managing campaign records, orchestrating phase transitions, and generating Plane tasks — currently has **zero unit test coverage** despite being critical infrastructure for spec-driven campaign workflows. This campaign adds comprehensive unit tests across four core modules (parsing, records, orchestration, builder) to validate correctness, establish testing patterns, and prevent regressions in future enhancements.

## Goals

1. **SpecFrontMatter parsing tests** — Test YAML front-matter extraction, field defaults, required-field enforcement, and phase-alias normalization (e.g., `test` → `test_campaign`). Cover valid specs, missing fields, malformed YAML, and timestamp parsing edge cases. Target: 12+ tests.

2. **Campaign record model tests** — Test CRUD operations, status lifecycle (active/completed/archived), record lookup by ID/slug, filtering by status, and list queries. Cover construction, serialization, and predicate queries. Target: 8+ tests.

3. **Phase orchestrator tests** — Test Plane issue state/label extraction, campaign ID parsing from issue bodies, terminal state detection, and error handling for missing or malformed metadata. Target: 10+ tests.

4. **Campaign builder tests** — Test spec → Plane task conversion with repo validation, area-keyword expansion, goal→issue mapping, and error recovery on constraint violations (missing fields, invalid repos). Target: 8+ tests.

## Constraints

- **Unit tests only:** No integration tests, no Plane API calls, no file I/O beyond temp fixtures. Mock external dependencies.
- **Code preservation:** Do not refactor spec_author source code; work with the current implementation as-is.
- **Single-repo focus:** All tests target OperationsCenter only; do not modify other repos.
- **File size:** Each test module under 300 lines to fit in a single executor run.
- **Testing patterns:** Follow existing OperationsCenter conventions (fixture naming, assertion style, per-module test files).
- **No coverage tools:** Write explicit tests for real behaviors, not to hit coverage metrics.
- **Error case coverage:** Each goal should include tests for both happy path and error/edge cases (malformed input, missing fields, invalid values).

## Success Criteria

- **Test count:** `pytest tests/unit/spec_author/` passes with ≥ 38 total tests across four modules (12+8+10+8).
- **All modules covered:** Four test files created (one per goal) with appropriate test classes, fixtures, and clear test names.
- **Execution speed:** Each test module executes in under 60 seconds on a single machine.
- **Error coverage:** Tests include both valid/happy-path cases and error cases (malformed YAML, missing fields, invalid values, edge cases).
- **No regressions:** Full test suite (`pytest tests/`) exits 0 with no broken existing tests.
- **Test quality:** Each test has a clear assertion and covers a single behavior; fixtures are reusable and documented.
