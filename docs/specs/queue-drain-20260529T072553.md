<!-- generated_by_run: f1133e42-1a11-4af3-9073-bbb374b374f3 -->

---
campaign_id: d5c7f1a9-2e3b-4d6c-8f9a-1b4c7e8d2a5f
slug: queue-drain-20260529T072553
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - board-unblock
  - worker-backends
  - workspace
  - board-worker
status: active
created_at: 2026-05-29T07:25:53Z
---

## Overview

The queue-drain improvement campaign hardens OperationsCenter's board-unblock mechanism, which manages task queue progression and task promotion across executor and worker backends. Recent releases fixed edge cases in task state detection (STALE_IN_REVIEW false positives), thin-goal task re-promotion loops, and workspace validation—but these fixes lack consistent observability and comprehensive test coverage. This campaign systematizes these improvements through enhanced metrics, unit tests, and regression prevention measures to ensure reliable queue-drain behavior.

## Repo Selection Analysis

**Selected: OperationsCenter** — Only repo with queue-drain ownership

Analysis of available repos:
- **OperationsCenter** — OWNS queue-drain logic. Core modules: `src/operations_center/entrypoints/maintenance/board_unblock.py` (22KB, 8 unblocking rules), `src/operations_center/entrypoints/board_worker/main.py` (board coordination), `src/operations_center/entrypoints/worker_backend_status/` (worker classification), `src/operations_center/adapters/workspace/` (state persistence). Recent commits d25e567, d5330a4, bf68fcf, a5a088d, 155c8fc all in OperationsCenter.
- **Custodian** — Provides audit policy and rules; does not implement queue-drain logic.
- **CxRP** — Generates decision cards; consumes OperationsCenter board decisions, not the creator.
- **SwitchBoard** — Lane/executor metadata; worker backend taxonomy comes from OperationsCenter taxonomy rules.
- **OperatorConsole, PlatformDeployment, VideoFoundry** — Consume queue-drain status; none implement drain logic.

Conclusion: OperationsCenter is the sole owner of queue-drain implementation; all other repos are consumers.

## Area Keywords Identification

Four focused improvement areas from codebase structure and recent commits:

1. **board-unblock** — Core queue-drain mechanism. File: `src/operations_center/entrypoints/maintenance/board_unblock.py`. Contains 8 rules (STALE_IN_REVIEW, GOAL_BACKLOG_PROMOTE, etc.). Recent fixes: STALE_IN_REVIEW false positives (commit a5a088d: "false-positive on tasks with open PRs"), GOAL_BACKLOG_PROMOTE thin-goal skipping (commit d5330a4).

2. **worker-backends** — Executor/worker classification and state. Files: `src/operations_center/entrypoints/worker/`, `src/operations_center/entrypoints/worker_backend_status/`. Recent fix: commit 155c8fc classifies aider_local/direct_local as direct worker backends, not executor lanes.

3. **workspace** — Workspace validation and task state persistence. File: `src/operations_center/adapters/workspace/`. Recent fix: commit d25e567 restores baseline-validation.json before task-branch checkout, preventing corruption.

4. **board-worker** — Board worker coordination and task promotion loops. File: `src/operations_center/entrypoints/board_worker/main.py`. Recent fix: commit bf68fcf stops thin-goal task re-promotion loop.

## Goals

1. **Add observability metrics to board_unblock rules** — Instrument the eight unblocking rules in `board_unblock.py` to emit structured logs and metrics (rule name, task count, state transitions) on each run. Track rule application frequency and success rate to identify patterns that cause queue stalls. Create `src/operations_center/tuning/board_unblock_metrics.py` to aggregate rule telemetry.

2. **Add unit tests for thin-goal re-promotion prevention** — Create `tests/unit/entrypoints/test_board_worker_thin_goal.py` covering: thin-goal rejection in `_claim_next()` (minimum 40 chars), Rule 7 (GOAL_BACKLOG_PROMOTE) skipping thin goals, and the board-worker gate preventing re-queuing of tasks that already failed the thin-goal check.

3. **Enhance Rule 5 (STALE_IN_REVIEW) PR state discovery** — Improve `board_unblock.py` Rule 5 to query GitHub PR state before declaring a task stale. Add a `pr_discovery_helper.py` module to batch PR lookups and cache results within the maintenance cycle, reducing API calls and false-positives.

4. **Add board health regression test** — Create `tests/integration/test_board_health_lifecycle.py` simulating a full task lifecycle (Ready → Running → Done, and Blocked recovery paths). Verify that all eight unblocking rules execute without errors and tasks transition to expected states.

## Constraints

- Do not modify the core unblocking logic of the eight rules; only add observability and testing.
- All new tests must be pure unit tests (mock Plane API calls; do not use live boards).
- Keep metric emission lightweight (structured logs, not heavy instrumentation that slows the maintenance loop).
- PR discovery in Rule 5 must be opt-in via a flag to preserve backward compatibility with offline operation.
- Do not add new unblocking rules; focus on hardening and testing the existing eight.

## Success Criteria

- `board_unblock_metrics.py` exports a `record_rule_application(rule_name, task_id, old_state, new_state)` function called by all eight rules.
- Metrics are emitted as JSON logs: `{"event": "rule_applied", "rule": "STALE_IN_REVIEW", "count": 3, ...}`.
- `tests/unit/entrypoints/test_board_worker_thin_goal.py` has ≥6 tests, all passing, covering thin-goal rejection and re-promotion prevention.
- `pr_discovery_helper.py` batches up to 10 PR lookups per cycle, caches results for 60 seconds, reduces GitHub API calls by ≥30% in mock tests.
- `tests/integration/test_board_health_lifecycle.py` simulates 5 task lifecycle scenarios, all pass without rule execution errors.
- All existing tests continue to pass; no regressions in `pytest tests/unit/entrypoints/maintenance/test_board_unblock.py`.
