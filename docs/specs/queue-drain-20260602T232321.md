<!-- generated_by_run: 44f79ad1-198d-4566-a95a-3c7c70d10232 -->

---
campaign_id: 3f8e1d4a-7c2b-4e9f-a1b6-c5d8e2f7a9c3
slug: queue-drain-20260602T232321
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - execution
  - reviewer
  - verdict-gate
status: active
created_at: 2026-06-02T23:23:21Z
---

## Overview

Strengthen test coverage for the verdict-gate loop, CI-green merge precondition, and cooldown state management in the reviewer subsystem. Recent hardened implementations (e6bfb8f, be1d2b33, 5b2e3dce) validate verdict routing, prevent premature merges, and manage stale backend cooldowns — verify these invariants hold under realistic scenarios.

## Goals

1. **Verdict-gate reconciliation tests** — Verify that verdicts from adversarial audit loop feed correctly into merge gate logic; test verdict timeout, override, and retry paths.
2. **CI-green precondition validation** — Assert that CI-green status blocks merge, verdicts gate the precondition check, and auto-fix loop doesn't bypass the gate.
3. **Cooldown state machine tests** — Validate that stale worker-backend cooldowns are detected and cleared; verify backend reactivation after cooldown window.
4. **End-to-end reviewer scenarios** — Test a full verdict-gate→merge flow with simulated verdict transitions, intermediate failures, and recovery.

## Constraints

- Tests must use the current executor/verdict schema (post-ADR-0005 vocabulary).
- Avoid mocking the verdict-gate loop; probe the live decision boundary.
- Reuse test fixtures from existing integration suites where available.
- Scope each test to a single subsystem component; avoid cross-subsystem brittleness.

## Success Criteria

- All 4 goals have passing test code committed to the repo.
- CI suite is green (no test regressions in existing suites).
- Each test includes a docstring explaining the invariant it validates.
- Verdict-gate, CI-green, and cooldown test files are discoverable in `tests/` or adjacent integration directory.
