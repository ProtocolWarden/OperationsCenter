<!-- generated_by_run: d10a40ab-86cc-4732-9f4e-d3aa25ecc9f0 -->

---
campaign_id: 8f7e4c2a-9b1d-4e6f-a3c2-d1b5f9e2a8c3
slug: queue-drain-20260529T072350
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - board-worker
  - board-unblock
  - workspace-management
status: active
created_at: 2026-05-29T07:23:50Z
---

## Overview

Recent board-worker and board-unblock improvements (thin-goal task handling, STALE_IN_REVIEW detection, workspace baseline validation) have introduced subtle promotion and detection logic that warrants hardening through expanded test coverage. This campaign adds regression tests for recent critical fixes and improves diagnostic visibility for queue-drain cycles.

## Goals

1. **Add regression test coverage for board-unblock STALE_IN_REVIEW fixes (commit a5a088d)** — Cover the false-positive detection issue with tasks that have open PRs; ensure the fix holds under concurrent task state changes. Add 8-10 edge-case tests in `tests/unit/board/test_board_unblock.py` covering PR state transitions and thin-goal interactions.

2. **Harden board-worker thin-goal task promotion guards (commits d5330a4, bf68fcf)** — Add regression tests for the GOAL_BACKLOG_PROMOTE thin-goal skip logic and the re-promotion loop prevention; verify guards hold under state mutations. Target: 6-8 new tests in `tests/unit/board/test_board_worker.py`.

3. **Create workspace baseline validation regression fixture (commit d25e567)** — Document and test the `.baseline-validation.json` restoration bug scenario (task-branch checkout ordering); add a minimal fixture-based regression test to prevent re-introduction. Target: 1-2 integration tests in `tests/integration/`.

4. **Add queue-drain diagnostic observability** — Implement a `board_state_summary()` helper that reports task counts by state (ready/running/drained), promotion eligibility, and thin-goal classification; emit on queue-drain trigger. Target: <80 lines of helper code + 3 tests, logged as JSON for monitoring.

## Constraints

- Modify only test files and diagnostic helpers; no changes to core board/workspace logic.
- All tests must use existing fixtures; do not introduce new external test dependencies.
- Regression tests must cite exact commits where issues were fixed (e.g., `a5a088d` for STALE_IN_REVIEW).
- Diagnostic output must be JSON-serializable for observability pipelines.
- Changes are diagnostic/defensive only; no functional behavior changes to board cycles.

## Success Criteria

- All four goals have passing implementations with 80%+ code coverage.
- Existing test suite remains green (3580+ tests unbroken).
- Each regression test includes a docstring referencing the fixed commit.
- Diagnostic helper is callable from queue-drain entrypoint without operational impact.
- No performance regression in board worker cycle time.
