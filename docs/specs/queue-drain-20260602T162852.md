<!-- generated_by_run: c2e98074-597c-40b0-ac62-b2c3268a0d15 -->

---
campaign_id: 7f558a6c-6ad4-44cf-940a-d86b3d5059f7
slug: queue-drain-20260602T162852
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - observer
  - test_coverage
  - collectors
  - alert_pipeline
  - instrumentation
status: active
created_at: 2026-06-02T17:22:01Z
---

## Overview

The observer module is a critical dependency tracking and health-monitoring subsystem in OperationsCenter, but current baseline coverage is 61.76% — well below the new 85% enforcement gate. Recent audit fixes in PR #213 and role-validation hardening (PR #217) provide a foundation for comprehensive edge-case testing. This campaign hardens test coverage for all observer collectors and the alert pipeline, adds performance instrumentation, and ensures the module meets the new coverage requirements while remaining performant under real workloads.

## Goals

1. **Observer collector edge-case test coverage** — Add unit tests for all collector modules (dependency_drift, execution_health, validation_history, task_state) covering normal operation, missing fields, malformed input, and timeout recovery. Target ≥85% line coverage across all collectors using recent audit-fix patterns as test templates. Success: 25–30 new tests, all collectors >85%.

2. **Alert pipeline integration testing** — Add integration tests for the collector→validation→alert flow: verify that audit failures propagate correctly, alerting thresholds trigger deterministically, and alert deduplication works under realistic event volumes. Cover both success and failure paths including observer timeout recovery. Success: 8–10 integration tests, all paths exercised, 0 flakiness.

3. **Observer performance instrumentation and baselines** — Instrument collection latency, validation time, and alert scheduling with structured metrics. Add performance regression tests for collection over varying dependency counts (baseline, 20-dep, 50-dep scenarios). Document expected latency SLAs for operators. Success: metrics exported, baselines <50ms per collection, regression detection in CI.

4. **Coverage gate compliance and documentation** — Reach 85% line coverage for all observer modules; document test strategy, edge cases covered, and assumptions for future maintainers. Add operator runbook for interpreting observer alerts and debugging collection failures. Success: observer module reports ≥85% coverage in CI; docs updated.

## Constraints

- **Scope:** Tests and instrumentation only — no changes to observer API, collector logic, or alert thresholds.
- **Allowed paths:** Add tests under `tests/unit/observer/` and `tests/integration/observer/`; add metrics instrumentation in `src/operations_center/observer/collectors/` and `src/operations_center/observer/service.py`; add docs to `docs/observer/`.
- **Avoid:** Refactoring observer state machine, changing alert trigger conditions, or modifying external dependencies (git, GitHub API, subprocess). Performance tests must use mocks or synthetic data, not live repos.
- **Performance overhead:** Instrumentation must add <5ms per collection cycle; measure and document overhead before declaring success.
- **Test isolation:** No tests should depend on real GitHub repositories, live API keys, or time-sensitive sleeps. All mocking must use established patterns (responses library, pytest fixtures, monkeypatch).

## Success Criteria

1. **Coverage gate met:** Observer module (src/operations_center/observer/) reports ≥85% line coverage in CI. Up from 61.76% baseline, closing the 23.24pp gap identified in Stage 0. All new tests integrated into existing test suite with 0 regressions.

2. **Edge-case test inventory:** 25–30 new unit tests added covering: missing required fields, malformed JSON, timeout recovery, concurrent collection, alert deduplication under load, validation failure propagation. Tests pass deterministically; 3 consecutive CI green runs.

3. **Integration pipeline tests:** 8–10 integration tests verify end-to-end collector→validation→alert flow. Cover both normal state changes and failure paths (timeouts, malformed audit reports). All tests <2 min runtime in CI; integration suite remains flake-free.

4. **Performance baselines and instrumentation:** Observer metrics (collection_latency_ms, validation_time_ms, alerts_scheduled) exported with proper labels (collector, repo, state). Baseline latency <50ms per cycle across all scenarios (baseline, 20-dep, 50-dep). Performance regression test detects >2x slowdown. Operator runbook documents SLA and troubleshooting steps.

5. **Documentation and operator guidance:** `docs/observer/testing-strategy.md` documents collector test patterns, edge cases covered, assumptions, and how to extend. `docs/operator_guides/observer-alerts.md` explains alert triggers, common failure modes, and recovery steps. Both docs linked from README.
