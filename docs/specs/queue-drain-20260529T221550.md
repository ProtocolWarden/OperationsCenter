<!-- generated_by_run: a7ddca24-a8c2-4516-9527-5a89b9100bdd -->

---
campaign_id: 8f7a2e1b-4c3f-47d9-9b8e-2c5f1a4d6e9c
slug: queue-drain-20260529T221550
phases:
  - implement
  - test
  - improve
repos:
  - OperationsCenter
area_keywords:
  - controller
  - state
  - testing
  - observability
status: active
created_at: 2026-05-29T22:15:50Z
---

## Overview

Incremental hardening of OperationsCenter controller subsystems following Work Order 0009 completion. Recent work removed duplicate logs and added PR conflict guards; these four tasks improve state resilience, test coverage, observability, and timeout configurability across the executor backend landscape.

## Goals

1. **State file atomic writes and corruption recovery** — Implement write-to-temp-then-rename atomic protocol for state.json to eliminate partial-write corruption on SIGTERM. Add 4–5 unit tests covering normal writes, SIGTERM interruption, file permissions, and journal replay on corrupt state. Success: atomicity tests pass; state is never corrupted by abrupt shutdown.

2. **PR state machine edge case test coverage** — Add 6–8 focused tests for review-watcher PR state machine edge cases: draft→open→draft cycles, mergeable status flips during CI, force-push followed by merge, and PR comment 404 retry. Verify recent CONFLICTING PR guard doesn't regress on these transitions. Success: all new tests pass; existing test suite unchanged.

3. **Structured watchdog cycle logging** — Add JSON-formatted cycle logs including `{ cycle_id, start_utc, end_utc, latency_ms, actions_applied }`. Record cycle start/end checkpoints for 4–5 sample iterations. Ensure <50ms overhead. Success: cycle logs are structured and overhead validated; operator can extract latency metrics.

4. **team_executor timeout instrumentation** — Add dispatch initiation timestamp and completion latency recording to team_executor adapter calls. Emit latency histograms in executor logs. Add 3–4 tests validating latency measurement accuracy. Success: latencies are measured, logged, and queryable; no regression in executor throughput.

## Constraints

- **Allowed:** Modify `src/operations_center/controller/` state file I/O; add tests under `tests/controller/` and `tests/review_watcher/`; instrument team_executor calls in `src/operations_center/executor/adapters/`.
- **Avoid:** Changes to controller dispatch policy, escalation rules, or executor governance; configuration system overhaul.
- State file atomicity must degrade gracefully if journal is unavailable; fallback to single best-effort write.
- Structured logging overhead must not exceed 50ms per cycle; measure before and after.
- Timeout instrumentation for team_executor only; defer dag_executor and critique_executor to future work.
- All tests must pass locally and in CI; no environment-specific skips.

## Success Criteria

1. **State file atomicity:** 4–5 unit tests pass for atomic writes, SIGTERM handling, file permissions, and corruption recovery. No data corruption observed in stress test (10 rapid iterations with simulated SIGTERM).

2. **PR state machine tests:** 6–8 new edge-case tests added and passing. All existing review-watcher tests still pass; code coverage for PR state transitions ≥85%.

3. **Watchdog cycle logging:** Cycle logs are emitted in JSON with all required fields (cycle_id, start_utc, end_utc, latency_ms, actions_applied). Overhead measurement confirms <50ms per cycle. Operator can query latencies from logs.

4. **team_executor timeout instrumentation:** Dispatch initiation and completion timestamps logged per call. Latency histograms computed and emitted. 3–4 tests validate accuracy within ±10ms tolerance. No regression in executor throughput (measured pre/post).
