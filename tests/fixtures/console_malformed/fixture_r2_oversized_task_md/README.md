# R2 Violation: File exceeds 100KB budget

This fixture represents the R2 violation where task.md exceeds the 100KB file size limit.
The R2 detector should report "count=1" with sample ".console/task.md exceeds 100KB budget".
