# R2 Violation: workers.yaml has YAML syntax error

This fixture represents the R2 violation where workers.yaml contains invalid YAML syntax.
The R2 detector should report "count=1" with sample ".console/workers.yaml has YAML syntax error".
